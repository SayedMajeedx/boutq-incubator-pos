project_id = "ikciahnuqhemvnyfvbyp"

# This function performs stronger order-scoped authorization internally and
# supports opaque sb_publishable keys plus one-time guest email tokens.
[functions.send-order-email]
verify_jwt = false
